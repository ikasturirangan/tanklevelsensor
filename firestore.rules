rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All client access goes through the authenticated Next.js backend.
    match /{document=**} { allow read, write: if false; }
  }
}
