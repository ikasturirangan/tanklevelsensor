import './globals.css';

export const metadata = {
  title: 'Terrace Tank · Water level',
  description: 'Water level and connection status for your Terrace Tank.',
};
export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
